{% extends 'baseBack.html.twig' %}

{% block title %}ReservationExpo{% endblock %}

{% block body %}
    <h1>ReservationExpo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodeReservatione</th>
                <td>{{ reservation_expo.codeReservatione }}</td>
            </tr>
            <tr>
                <th>NbPlace</th>
                <td>{{ reservation_expo.nbPlace }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_expo_indexBack') }}">back to list</a>

    <a href="{{ path('reservation_expo_editBack', {'codeReservatione': reservation_expo.codeReservatione}) }}">edit</a>

    {{ include('reservation_expo/_delete_form.html.twig') }}
{% endblock %}
