{% extends 'base.html.twig' %}

{% block title %}PendingOrders{% endblock %}

{% block body %}
    <h1>PendingOrders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPendingorder</th>
                <td>{{ pending_order.idPendingorder }}</td>
            </tr>
            <tr>
                <th>Orderid</th>
                <td>{{ pending_order.orderid }}</td>
            </tr>
            <tr>
                <th>Innonumber</th>
                <td>{{ pending_order.innonumber }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ pending_order.quantity }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ pending_order.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pending_orders_index') }}">back to list</a>

    <a href="{{ path('pending_orders_edit', {'idPendingorder': pending_order.idPendingorder}) }}">edit</a>

    {{ include('pending_orders/_delete_form.html.twig') }}
{% endblock %}
