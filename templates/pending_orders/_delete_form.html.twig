<form method="post" action="{{ path('pending_orders_delete', {'idPendingorder': pending_order.idPendingorder}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pending_order.idPendingorder) }}">
    <button class="btn">Delete</button>
</form>
