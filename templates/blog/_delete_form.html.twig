<form method="post" action="{{ path('blog_delete', {'idblog': blog.idblog}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.idblog) }}">
    <button class="btn">Delete</button>
</form>
