<form method="post" action="{{ path('cart_delete', {'idcart': cart.idcart}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cart.idcart) }}">
    <button class="btn">Delete</button>
</form>
