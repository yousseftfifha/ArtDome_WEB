<form method="post" action="{{ path('endroit_delete', {'idEndroit': endroit.idEndroit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ endroit.idEndroit) }}">
    <button class="btn">Delete</button>
</form>
