<form method="post" action="{{ path('exposition_delete', {'codeExpo': exposition.codeExpo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exposition.codeExpo) }}">
    <button class="btn">Delete</button>
</form>
