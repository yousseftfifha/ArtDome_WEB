<form method="post" action="{{ path('commentaire_delete', {'idComment': commentaire.idComment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idComment) }}">
    <button class="btn">Delete</button>
</form>
